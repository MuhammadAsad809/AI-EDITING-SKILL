# Scripts

`validate_plan.py` performs deterministic checks on a Markdown editing plan: required output headings, timecode patterns, and effect-purpose language. It is a lint helper, not a substitute for editorial judgment.
